name: nox
on:
    - push
jobs:
    run-nox:
        runs-on: ubuntu-20.04
        steps:
            - uses: actions/checkout@v2
            - uses: excitedleigh/setup-nox@v2.0.0
            - run: nox
